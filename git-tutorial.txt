### Start
0. Create repoistry in github
	 mkdir filepath
	 cd filepath
1. git init
2. git remote add origin *github.git
	 git add file
	 git add ./*
	 git commit -m "update"
3. git push origin master 
4. git pull *.git
 	git pull
	
	from website


### Add a tag
	git tag -a taginfo -m "paper submitted"
	git tag
	git push origin taginfo

